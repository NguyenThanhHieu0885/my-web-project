# --- Vẫn chặn file rác hệ thống ---
.DS_Store
Thumbs.db

# --- Vẫn chặn thư viện (BẮT BUỘC) ---
# Lý do: Docker sẽ tự cài cái này. Up lên sẽ gây xung đột Win/Mac/Linux
/backend/vendor/
/frontend/node_modules/

# --- CÁC PHẦN ĐÃ BỎ CHẶN (Để up lên Git) ---
# Đã xóa dòng /backend/.env
# Đã xóa dòng /backend/database/*.sqlite